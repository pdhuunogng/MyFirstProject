/*syntax
    data_type name_of_function (parameters)
    {
        statements;
    }
*/

/*
    data_type result = name_of_function(param1, param2,...)
*/

//pass by VALUE - change the copied 
//pass by REFERENCES - change the original 

//local
/*
global: sử dụng external để access tới 1 biến nằm ở 1 chương trình khác
          (chia sẻ global variables)
          khi compile, nhập tất cả chương trình có liên quan tới
static global: không cho phép chia sẻ 
*/

//static
//auto 
//register: stored variable in CPU, not in memory 